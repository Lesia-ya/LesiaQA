[pytest]
markers =
    change: Tests that modify name of user
    check: Tests that check users name
    http: Tests that check HTPP Protocol
    api: Tests that check that user exists
    database: Tests that check connection to database
    ui: Tests that check user interface
